import { HighlightmyDirective } from './highlightmy.directive';

describe('HighlightmyDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightmyDirective();
    expect(directive).toBeTruthy();
  });
});
